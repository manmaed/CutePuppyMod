package net.manmaed.cutepuppymod.world;

/**
 * Created by manmaed on 28/04/2021.
 */
public class WorldGenMobs {

    //TODO: FIX WORLD GEN CLASS
    /*public static void withPassiveMobs(BiomeLoadingEvent builder) {
        if (!CPConfig.DISABLE_RED_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.RED.get(), CPConfig.RED_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_BLUE_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.BLUE.get(), CPConfig.BLUE_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_GREEN_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.GREEN.get(), CPConfig.GREEN_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_YELLOW_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.YELLOW.get(), CPConfig.YELLOW_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_PURPLE_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.PURPLE.get(), CPConfig.PURPLE_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_STEVE_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.STEVE.get(), CPConfig.STEVE_CHANCE.get(), 2, 4));
        }
        else if (!CPConfig.DISABLE_HEROBRINE_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.HEROBRINE.get(), CPConfig.HEROBRINE_CHANCE.get(), 0, 1));
        }
    }

    public static void withHostileMobs(BiomeLoadingEvent builder) {
        if (!CPConfig.DISABLE_ENDER_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.MONSTER).add(new MobSpawnInfo.Spawners(CPEntityTypes.ENDER.get(), CPConfig.ENDER_CHANCE.get(), 2, 3));
        }
    }

    public static void withEndMobs(BiomeLoadingEvent builder) {
        if (!CPConfig.DISABLE_ENDER_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.MONSTER).add(new MobSpawnInfo.Spawners(CPEntityTypes.ENDER.get(), (CPConfig.ENDER_CHANCE.get()), 4, 10));
        }
    }

    public static void withNetherMobs(BiomeLoadingEvent builder) {
        if (!CPConfig.DISABLE_ENDER_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.MONSTER).add(new MobSpawnInfo.Spawners(CPEntityTypes.ENDER.get(), (CPConfig.ENDER_CHANCE.get()), 1, 1));
        }
        else if (!CPConfig.DISABLE_HEROBRINE_SPAWN.get()) {
            builder.getSpawns().getSpawner(EntityClassification.CREATURE).add(new MobSpawnInfo.Spawners(CPEntityTypes.HEROBRINE.get(), CPConfig.HEROBRINE_CHANCE.get(), 0, 1));
        }
    }*/
}
